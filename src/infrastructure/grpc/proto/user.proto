syntax = "proto3";

package user;

service UserService {
    rpc login (LoginUserRequest) returns (LoginUserResponse);
}

message LoginUserRequest {
    string username = 1; // Add username
    string password = 2; // Add password
}

message LoginUserResponse {
    bool success = 1; // Indicate if login was successful
}
